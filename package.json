{
  "name": "frontend-interview-atlas",
  "version": "1.0.0",
  "description": "Frontend interview knowledge base: crawls real interview experiences, synthesizes them into study material, and serves it through a dashboard",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "build": "npm run build --prefix dashboard-react",
    "start": "node server.js",
    "dev": "npm run dev --prefix dashboard-react",
    "crawl": "./run_crawler.sh",
    "test": "npm run build && node --test && .venv/bin/python3 -m unittest discover -s test -p 'test_*.py'"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
